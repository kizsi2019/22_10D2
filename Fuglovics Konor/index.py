months = ['January', 'February', 'March', 'April', 'May']

index = 0
'''for month in months:
    print(month)
    index = index + 1'''

'''for index in range(len(months)):
    print(index, months[index])'''

for month in months:
    month = month.upper()
    print(month)